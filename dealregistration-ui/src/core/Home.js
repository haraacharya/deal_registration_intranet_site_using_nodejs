import React from "react";
import Layout from "./Layout"
import {API} from "../config"

const Home = () => {
    return (
        <Layout>
            {API}
        </Layout>
        )
};

export default Home;