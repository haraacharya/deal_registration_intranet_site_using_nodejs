import React from "react";
import Layout from "../core/Layout"

const Signin = () => {
    return (
        <Layout title="Signin" description="Signin to deal registration site">
            
        </Layout>
    )
};

export default Signin;